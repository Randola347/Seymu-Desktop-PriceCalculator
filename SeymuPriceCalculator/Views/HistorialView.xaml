<UserControl x:Class="SeymuPriceCalculator.Views.HistorialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Cotizaciones"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#2D6A27"
                   Margin="0,0,0,15"/>

        <TextBox Grid.Row="1"
                 Height="38"
                 Margin="0,0,0,15"
                 VerticalContentAlignment="Center"
                 Text="Aquí luego pondremos búsqueda por cliente o fecha."/>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>